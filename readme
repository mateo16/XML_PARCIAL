Para poder correr el script es necesario tener una API key para Congress.gov 
configurada en la variable de entorno CONGRESS_API. También es necesario tener java,
la biblioteca saxon para java instalada (recomendamos que sea la versión 11 que es la que tenemos nosotros,
pero debería funcionar con versiones superiores)
con las variables PATH y CLASSPATH configuradas.
Si excede el límite de consultas de la api el script no funcionará,
o funcionará con los xml obtenidos anteriormente si no borro los resultados anteriores de la carpeta data.